---
interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}
const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-6xl px-5 sm:px-8", className]}>
  <slot />
</Tag>
